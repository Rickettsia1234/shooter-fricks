public enum SceneName
{
    Title,
    Game,
    Dead,
    Clear,
}

public enum GameState
{
    Title,
    Setting,
    Game,
    Dead,
    Clear,
}

public enum PlayerType
{
    Plain1,
    Plain2,
    Plain3,
}